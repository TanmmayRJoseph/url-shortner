global:
  scrape_interval: 4s # how often to scrape targets by default

scrape_configs:
  - job_name: "backend"
    static_configs:
      - targets: ["backend:5000"]
        labels:
          instance: "backend"
